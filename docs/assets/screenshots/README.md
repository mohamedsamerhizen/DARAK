# DARAK Screenshot Assets

This folder is reserved for real reviewed screenshots from the running DARAK backend.

No screenshot image is committed in the GitHub presentation pass because the real Swagger capture attempt exposed a Swagger schema-generation error caused by duplicate `SupportDashboardResponse` DTO schema names. Failed Swagger pages and placeholder screenshots should not be committed.

After Swagger loads successfully, save reviewed captures here and update the README links deliberately.
